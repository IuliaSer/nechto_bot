spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  datasource:
    show_sql: true
    url: jdbc:postgresql://localhost:5430/postgres?currentSchema=nechto
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
  sql:
    init:
      mode: never
  liquibase:
    default-schema: nechto
    clearCheckSums: true
    change-log: classpath:db/changelog/db.changelog-master.yaml
  cloud:
    openfeign:
#      httpclient:
#        hc5:
#          enabled=false
      multipart:
        enabled=true
      okhttp:
        enabled: true
      client:
        config:
          telegram: # name = "telegram" в @FeignClient
            loggerLevel: FULL

  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true

  logging:
    level:
      org:
        springframework:
          security: debug
telegrambot:
  webHookPath: https://df53e5ff34d4.ngrok-free.app
  userName: nechto21_bot
  botToken: 8086975968:AAEWRBWduBoPDT7TUJpFlO5s_fOVvkvqSvY
server:
  port: 8000

logging:
  level:
    nechto.telegram_bot.TelegramFeignClient: DEBUG
    feign: DEBUG

feign:
  client:
    config:
      telegram:
        loggerLevel: FULL
